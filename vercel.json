{
    "version": 2,
    "builds": [
      { "src": "tamaChat.py", "use": "@vercel/python" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/tamaChat.py",
      "headers": {
        "Access-Control-Allow-Origin": "*"
      } }
    ]
  }